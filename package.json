{
    "private": true,
    "type": "module",
    "scripts": {
        "rebase": "npm-run-all  wipe migrate seed",
        "seed": "php artisan db:seed",
        "dev": "vite",
        "build": "vite build",
        "serve": "php artisan serve --port=8008",
        "migrate": "php artisan app:group-migrate",
        "wipe": "php artisan db:wipe",
        "start": "npm-run-all --parallel dev serve"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0",
        "npm-run-all": "^4.1.5"
    }
}
